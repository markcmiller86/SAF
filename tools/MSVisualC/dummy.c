/*
This is a dummy file that is used for several MSVisual projects
whose only purpose is some post-build steps. I am creating these
projects as console projects rather than utility projects, because
the post-build steps for utility projects never seem to happen.
*/
#include <stdio.h>
void main()
{

}